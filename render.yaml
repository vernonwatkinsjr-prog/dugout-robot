services:
  - type: web
    name: vs-dugout-statcast
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn statcast_server:app --timeout 180 --workers 1
    healthCheckPath: /health
